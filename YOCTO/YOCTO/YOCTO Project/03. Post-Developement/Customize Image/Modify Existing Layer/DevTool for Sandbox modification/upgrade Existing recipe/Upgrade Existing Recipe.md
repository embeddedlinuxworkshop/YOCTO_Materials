



```bash

> devtool upgrade <recipe_name> --version new_version
> devtool edit-recipe <recipe_name> # change recipe if needed.
> devtool build <recipe_name>
> devtool deploy-target <package name> root@192.16.7.2 # target emulator.
> devtool undeploy-target <package name> root@192.168.7.2
> devtool finish python3-flask <layer>
> rm -rf workspace/sources/<package name>
```

